
# Memory File: NetHealth Religion Project Revival
This file serves as a comprehensive memory log to help resume work, understand the analytical pipeline, and maintain historical context for future sessions.

---

## 0. Repository State — READ FIRST (as of 2026-09-24)

**IMPORTANT — the "cleanup pass" narrative below (deleting `update_regression_models*.R`, `get_coefs.R`, `get_wave3_coefs.R` as "superseded, ad-hoc iterations") turned out to be WRONG for the regression tables specifically.** A later same-day session (see Section 6) discovered those deleted scripts were actually the *source* of the numbers published in `Tabs/tbl-wave3-reg.tex` and `Tabs/tbl-pooled-interaction-reg.tex` at the time, and one of them (`as.numeric(as.factor(close))`) contained a real ordinal-coding bug (alphabetizes intimacy categories instead of using the true Distant→Especially Close scale). The canonical `Code/02_dyadic_models*.R` scripts did **not** reproduce the published Wave 3 Model 2/3 numbers before that session fixed this — see Section 6 for the full resolution. **Moral: before trusting a `Tabs/*.tex` number, always confirm it against a fresh run of the corresponding canonical `Code/*.R` script; a checked-in table and the checked-in script that supposedly produces it can silently diverge.**

**The Overleaf merge (previously documented here) is resolved.** `git status` is clean on `my-new-branch`. `paper.tex` compiles via `latexmk -pdf paper.tex` with no errors.

**Project folder was reorganized on 2026-09-24 (cleanup pass):**
- Deleted 23 root-level scratch scripts (`update_*.R`, `run_pbc_analysis*.R`, `get_coefs.R`, `get_wave3_coefs.R`, `check_waves.R`, `fix_all_tables_final.py`, `update_analysis_qmd_facet.py`, `generate_plot.R`, `run_plot_update.R`) — these were superseded, ad-hoc iterations of what is now cleanly implemented in `Code/01`–`Code/07`. Still recoverable via git history if ever needed.
- Moved `analysis.qmd`/`analysis.html` to `Archive/` — this Quarto document was described by the old README as the "primary" pipeline, but was never mentioned anywhere in this memory file and is **not** what produced the current paper's tables/figures (that's `Code/*.R`). Treat `Code/` as the sole canonical pipeline going forward; `README.md` was rewritten accordingly.
- Moved orphaned/superseded outputs to `Archive/orphaned_tables/` and `Archive/orphaned_plots/`: `tbl-interaction-reg.tex`, `tbl-pooled-reg.tex`, `tbl-yules-q.tex` (all superseded by `tbl-pooled-interaction-reg.tex` / `tbl-yules-q-null.tex`, confirmed not `\include`-d in `paper.tex`), plus duplicate `plot_*.png` files and stray root `plot.jpg`/`plot.png` (all superseded by the `fig-*.png` files actually `\includegraphics`-ed in `paper.tex`).
- Moved the 6 standalone HTML tables from `Code/06_save_tables_html.R` (`Table_A1`...`Table_C4`) to `Archive/supplementary_html_tables/` — user confirmed these are leftover scratch output, not journal supplementary materials.
- Added `.gitignore` (LaTeX build byproducts `*.aux/.bbl/.blg/.fdb_latexmk/.fls/.log/.out`, `.Rhistory`, `.Rproj.user/`) and untracked those files from git (they remain on disk, regenerated by `latexmk`). `paper.pdf` itself remains tracked.
- Current root-level contents: `AGENTS.md`, `README.md`, `paper.tex`/`.pdf`/bib, `Code/`, `Data/`, `Plots/`, `Tabs/`, `Archive/`, `reviewer-comments.docx`, `reviewer_comments_dump.txt`, `nethealth_religion.Rproj`.
- **Full reconciliation completed 2026-09-24.** The cleanup commits were pushed to `origin/my-new-branch` (GitHub), but the first push to `overleaf` was rejected: a co-author was actively editing prose directly in the Overleaf web editor throughout the session, landing **three separate new commits** on `overleaf/main` (`93d63f5`, `6aeaab2`, `757cf3f`) that had to be fetched, diffed, and merged one at a time as each was discovered mid-push. All three were prose-only edits to `paper.tex` (Introduction/Study Setting, Wave 3/pooled-regression, and Discussion sections — wording tweaks, paragraph breaks) plus two new `references.bib` entries (`mcfarland2014network-735`, `hachen2022generators-923`). Every merge was conflict-free since the cleanup commits never touched `paper.tex`/`references.bib`. `paper.pdf` was regenerated via `latexmk -pdf paper.tex` after each merge (20 pages, no errors) and committed alongside. **`origin/my-new-branch`, `overleaf/main`, and local `my-new-branch` are now verified identical at commit `6ed6d2a`.**
- **Lesson for future sessions:** always `git fetch overleaf && git diff HEAD overleaf/main` immediately before *every* push to `overleaf`, not just once at session start — a co-author can be actively editing in the web UI in real time, so new commits can appear between successive push attempts within the same session. After a rejected push, re-fetch, re-diff, merge, and try again rather than assuming the first sync captured everything.
- **`git push overleaf` branch-name gotcha (fixed 2026-09-24):** Overleaf's git remote only ever has a single branch, `main`, and rejects any push that tries to create/update a differently-named remote branch with `remote: error: wrong branch` / `hint: You can't push any new branches.` Since local work happens on `my-new-branch`, a bare `git push overleaf` used to fail. Fixed permanently by adding a `push` refspec to the `[remote "overleaf"]` block in `.git/config`: `push = refs/heads/my-new-branch:refs/heads/main`. This makes `git push overleaf` (and `git pull`/`fetch`, unaffected) always map local `my-new-branch` onto remote `main` without needing `git push overleaf my-new-branch:main` spelled out each time. The `origin` and `upstream` remotes are untouched. If `my-new-branch` is ever renamed, this refspec must be updated to match.

---

## 1. Project Context & Objectives
This project analyzes **religious homophily and friendship formation** among college students using the **NetHealth longitudinal dataset** (from the University of Notre Dame).

The project has been completely restructured to address and resolve severe methodological flaws identified by two journal reviewers:
1.  **Resolved the Group Size Artifact:** Corrected a calculation in the original draft that aggregated individual-level "normalized EI" indices. By shifting to group-level, baseline-adjusted **Yule's Q**, we demonstrated that **all religious groups exhibit robust homophily** in every wave — though see Section 3 below on the limits of this claim.
2.  **Addressed Confounding Foci & Alternative Homophilies:** Shifted from aggregate, ego-level regressions to **dyadic (tie-level) logistic regressions**. This allows us to control for physical/social foci (same dorm, roommates) and demographic homophilies (gender, race).
3.  **Formal Causal Mediation & Scaling Artifacts:** Conducted formal causal mediation analysis to show that physical foci (dorms/roommates) do *not* mediate secular ("No Religion") homophily, proving that earlier nested model coefficient drops were merely logit rescaling artifacts.
4.  **Longitudinal & Intimacy Interactions via Marginal Effects:** Evaluated interactions of religious affiliation over time (Waves 3-8) and across friendship intensity (1-4 scale) using proper probability-scale marginal effects to avoid non-linear scaling issues.
5.  **Removed ERGMs, then confirmed why:** Discarded whole-network Exponential Random Graph Models. This was originally done because subsetting to within-cohort ties decimated the minority sample size. A 2026-09-23 session independently confirmed ERGMs are infeasible for a different, more fundamental reason: only ~32% of nominated alter IDs in the network survey resolve to actual surveyed egos, so there is no valid closed sociomatrix to fit a whole-network model on.
6.  **Updated Dyadic Regression Scripts (earlier revision):** Added `discuss_num` (Religious Salience) as a control in the pooled regression models, and standardized LaTeX table variable labeling (using "Tie: " prefix for non-ego variables).

---

## 2. Dataset Information

### A. Core Data Files (Located in `Data/`)
*   **`BasicSurvey(3-6-20).csv`:** Ego-level longitudinal survey.
    *   *Primary Key:* `egoid`
    *   *Key Variables:*
        *   `yourelig_1`: Baseline religious affiliation (Catholic, No Religion, Other Religion, Protestant).
        *   `gender_1`: Ego gender (Woman, Man).
        *   `race_1`: Ego race (White, African-American, Asian-American, Latino/a, Foreign Student, Other).
        *   `discussrelig_2`: Frequency of discussing religion (used as religious salience proxy).
*   **`NetWorkSurvey(2-28-20).csv`:** Longitudinal ego-network survey detailing nominated ties.
    *   *Keys:* `egoid` (the nominator), `alterid` (the nominated friend). **Note:** only ~32% of `alterid` values in a given wave resolve to a known `egoid` in the Basic Survey — most nominated alters are not themselves study participants. This rules out whole-network models (ERGMs) that require a closed node set.
    *   *Key Variables:*
        *   `wave`: Wave indicator (`Wave1` to `Wave8`).
        *   `altrelucat`: Nominator-nominee relationship context (`Student` denotes on-campus peers).
        *   `altrelig`: Alter religion (Catholic, NoReligion, OtherReligion, Protestant).
        *   `altsex`: Alter gender (Woman, Man).
        *   `altwhite`, `altblack`, `altasian`, `althisla`: Logical variables representing alter race/ethnicity.
        *   `roommates`: Logical variable indicating if the alter is a roommate.
        *   `samedorm`: Logical variable indicating if the alter lives in the same dorm.
        *   `close`: Friendship intimacy, modeled as an ordered 1-4 scale (Distant, Less Than Close, Merely Close, Especially Close).

### B. Known group sizes (baseline, N=722, denominator includes <1% missing)
Catholic 72.7%, No Religion 12.3%, Protestant 9.8%, Other Religion 4.6%. (An earlier draft inconsistently reported 73%/12.4%/9.9% in one section using a different denominator — this was reconciled on 2026-09-23; use the percentages above going forward.)

---

## 3. Mathematical & Statistical Architecture

### A. Group-Level Yule's Q (Longitudinal Trajectory)
Yule's Q is a margin-free metric computed from the $4 \times 4$ mixing matrix $M$. Its *expected value* under random mixing is 0 regardless of group size (unlike the E-I index, whose expectation is $1-2p$). This makes it valid for testing, **within each group**, whether observed mixing departs from that group's own chance baseline.

**Important limitation confirmed by simulation (2026-09-23):** Yule's Q's *magnitude* is NOT comparable across groups of very different sizes, even when the true underlying preference is identical. A simulation with an identical true excess-preference parameter across two groups showed mean Q rising mechanically from 0.55 (50/50 split) to 0.86 (5% minority) purely from group-size composition. Separately, a degree-preserving permutation-null test (fixed ego degree/religion, alters resampled from the wave's opportunity pool, 1,000 reps) showed that for small groups (e.g., Other Religion), the null distribution of Q is itself biased away from zero (null means around -0.09 to -0.38 across waves) and much wider than for large groups. **Do not use raw Q, or even permutation z-scores, to rank groups against each other.** Use Q only to establish that each group individually exceeds its own chance baseline (see `Code/07_yules_q_permutation_test.R` and `Tabs/tbl-yules-q-null.tex`).

### B. Dyadic Logistic Regression with Opportunity Offset
To model the likelihood of a tie being homophilous (`same_religion` $= 1$) while adjusting for baseline group proportions, we use a logistic regression on the tie-level dataset with a **mathematical opportunity offset**:
$$\operatorname{logit}(P(\text{same\_religion} = 1)) = \beta_0 + \beta_1 \text{EgoReligion} + \mathbf{X}\mathbf{\beta} + \text{opportunity\_offset}$$
Where the `opportunity_offset` $= \log\left(\frac{p}{1 - p}\right)$.

**Important limitation confirmed by bootstrap analysis (2026-09-23):** the offset corrects *bias* in the point estimate, but does not equalize *precision* across groups of very different sizes. A bootstrap resampling analysis (resampling egos with replacement within each religious group) at Wave 3, and again pooled across Waves 3-8, showed the ranking implied by the logit coefficients (Other Religion ≫ Protestant ≈ No Religion) is **not statistically distinguishable** — all pairwise minority-vs-minority 95% CIs for excess in-group tie rate include zero. Other Religion has only 16 egos at Wave 3 and 19-22 pooled across all 6 waves; pooling more waves does not fix this because the constraint is the number of distinct people in the group, not the number of observed ties. **Report ego/tie counts alongside every regression table** (now done in `Tabs/tbl-wave3-reg.tex` and `Tabs/tbl-pooled-interaction-reg.tex`) so readers can judge precision themselves.

### C. Mediation & Marginal Effects
*   **Mediation (`mediation` package):** Used to formally test and reject the hypothesis that structural foci mediate the homophily of the No Religion group. The ACME estimates for dorm co-residence and race homophily are statistically indistinguishable from zero, meaning the No Religion group's homophily coefficient (significant across all three Wave 3 models and the pooled model) is a genuine, unmediated association — not an artifact of physical/demographic sorting. (An earlier paper draft had an internal contradiction claiming both that No Religion "does not exhibit significant active homophily" and that it does; this was resolved on 2026-09-23 — No Religion IS significant everywhere it's tested.)
*   **Marginal Effects (`marginaleffects` package):** Used to correctly evaluate interaction terms (Time × Religion and Intimacy × Religion) in logistic regression by computing excess probabilities while holding the opportunity offset at 0.

---

## 4. Code Pipeline & Script Executions (Located in `Code/`)

1.  **`Code/prep_data20240603.R`** — Legacy cleaning script.
2.  **`Code/01_longitudinal_descriptives.R`** — Computes group-level Yule's Q across 8 waves.
3.  **`Code/02_dyadic_models.R`** — Wave 3 nested dyadic logistic regressions (opportunity offset), Models 1-4 (Model 4 adds ego/alter degree controls, 2026-09-24 — see Section 6). Computes and prints group-level ego/tie sample sizes per model, matching `Tabs/tbl-wave3-reg.tex`. This is the verified, ground-truth source for that table as of 2026-09-24.
4.  **`Code/02_dyadic_models_pooled.R`** — Pooled Waves 3-8 dyadic logistic regressions with clustered SEs, Models 1-3 (Model 3 adds `close_num` and degree controls, 2026-09-24 — see Section 6). Computes and prints group-level ego/tie sample sizes for the fully-adjusted analytic sample, matching `Tabs/tbl-pooled-interaction-reg.tex`. This is the verified, ground-truth source for that table's Pooled columns as of 2026-09-24.
5.  **`Code/02_dyadic_models_interaction.R`** — Time × Religion interaction model, now also including `close_num` and ego/alter degree controls (2026-09-24). Ground-truth source for the "Interaction (+Degree)" column of `Tabs/tbl-pooled-interaction-reg.tex`.
6.  **`Code/04_plot_opportunity_stability.R`**, **`Code/05_plot_active_coefficients.R`**, **`Code/06_save_tables_html.R`** — Plotting/table utilities (note: `06_save_tables_html.R` writes standalone HTML supplementary tables with hardcoded numbers; it is not the source of the `.tex` tables included in `paper.tex` and may drift out of sync — check before trusting its output).
7.  **`Code/07_yules_q_permutation_test.R`** (new, 2026-09-23) — Degree-preserving permutation null test for Yule's Q across all 8 waves; writes `Data/yules_q_permutation_null.csv` and `Tabs/tbl-yules-q-null.tex`.

*(ERGM script deprecated — see Section 1, item 5, for why this has been independently reconfirmed as the right call.)*

---

## 5. Manuscript Status (`paper.tex`) — as of 2026-09-23

A full reviewer-response audit was conducted this session (see conversation history for the two reviewers' original comments in `reviewer_comments_dump.txt` / `reviewer-comments.docx`). Key outcome: the prior revision (Yule's Q + opportunity-offset dyadic regressions) genuinely fixed some issues but **oversold what the new methods could establish** — specifically, Reviewer 2's core objection (baseline-adjusted homophily measures mechanically favor smaller groups regardless of true preference) was not actually resolved by switching from EI to Yule's Q; this was confirmed by simulation. The manuscript has been revised accordingly:

- **Abstract, Wave 3 regression section, pooled regression section, marginal-effects paragraph, Discussion:** all reframed from "minorities show a ranked hierarchy of active homophily (Other Religion > Protestant ≈ No Religion), supporting MDH" to **"non-Catholic groups collectively exceed the Catholic baseline; the specific ordering among non-Catholic subgroups is not statistically reliable, given Other Religion's small N."**
- **Methods (§3.5.1, Yule's Q definition):** corrected claims that these methods provide "unbiased, comparative" cross-group comparisons — added explicit caveats distinguishing point-estimate bias (which is corrected) from cross-group estimation precision (which is not equalized).
- **"Universal inbreeding homophily" claim:** now anchored to a formal permutation test (`Tabs/tbl-yules-q-null.tex`) rather than eyeballed Q magnitudes.
- **No Religion internal contradiction:** ~~resolved — the manuscript now consistently states No Religion shows a stable, significant, unmediated homophily effect in every specification (Wave 3 Models 1-3, pooled model).~~ **SUPERSEDED 2026-09-24 (Section 6): this "resolution" was itself built on Wave 3 Model 2/3 numbers that couldn't be reproduced from any script in the repo.** With the corrected/canonical models, No Religion is significant in the pooled model but NOT in Wave 3 Models 2-4 (once tie-level structural controls are added). See Section 6 for the current, verified state.
- **Language:** softened several unsupported "preference"/"actively prefer"/"driven by identity salience" phrasings in the Introduction, Study Setting, and Yule's Q definition, per Reviewer 2's objection that preference cannot be inferred from observational tie-formation data. Established field terminology ("choice homophily" / "induced homophily" as a taxonomy from cited literature) was left intact.
- **Minor fix:** reconciled a 12.3%/12.4% rounding inconsistency for the No Religion group's population share (see Section 2B above).
- **Compiled successfully** via `latexmk -pdf paper.tex` (20 pages, no errors, no undefined refs) as of the last check before the merge — note the merge in Section 0 may have reverted `paper.tex`/`paper.pdf` to a conflicted state; recompile after resolving.

### Still open / not yet addressed
- ~~**Reviewer 1's activity/popularity (degree) confound**~~ **RESOLVED 2026-09-24 — see Section 6.**
- A handful of "preference"/"choice" instances remain in the Methods section framing (e.g., §3.3 Opportunity Offset Formulation) that are technical/hypothetical framing rather than assertions about this paper's own findings — lower priority than the Introduction/Discussion instances already fixed, but worth a final pass.
- ~~**No formal, reproducible mediation analysis exists for the No Religion group.**~~ **RESOLVED 2026-09-24 — see Section 7.** `Code/03_mediation_analysis.R` now exists and reproduces new, honest ACME estimates; `paper.tex` cites them.
- **The pooled model's No Religion effect (significant) vs. the Wave 3 cross-section's No Religion effect (not significant once structural controls are added) are not reconciled.** The manuscript currently reports both without adjudicating (see Discussion). Worth investigating whether this is a power issue (pooled N=676 ties/37 egos vs. Wave 3 N=166 ties/25 egos) or a genuine wave-3-specific pattern.

---

## 6. Reviewer 1 Degree Confound — Resolved, and a Table/Script Mismatch Bug Found & Fixed (2026-09-24)

**Task:** address Reviewer 1's remaining objection that dyadic regression coefficients might reflect ego activity or alter popularity ("main effect" degree differences by religious group) rather than genuine homophily.

**What was added:** every dyadic model script now computes, per wave, each ego's **log out-degree** (`log_ego_degree` = log of the number of alters that ego nominated that wave) and each alter's **log in-degree / popularity** (`log_alter_pop` = log1p of the number of times that alter was nominated that wave). Both are computed from the **full, unfiltered** `NetWorkSurvey.csv` roster for that wave (before restricting to `altrelucat == "Student"` / known `altrelig`), since activity/popularity are properties of nomination behavior, not of the analytic homophily subsample.
- `Code/02_dyadic_models.R`: added as **Model 4** (nested after salience, Model 3).
- `Code/02_dyadic_models_pooled.R`: added as **Model 3** (nested after salience, Model 2); also renamed old "Model 2" output variable naming stays the same but the formula now includes `close_num` too (see bug fix below).
- `Code/02_dyadic_models_interaction.R`: added directly into the single interaction model's formula.
- **Finding:** degree/popularity terms are only marginally significant ($p \approx 0.07$–$0.16$) and do **not** meaningfully attenuate the Protestant or Other Religion coefficients in any specification (Wave 3 or pooled). The already-null No Religion Wave-3 coefficient stays null. Reviewer 1's confound is addressed: opportunity-adjusted homophily is not an artifact of who's more active or more popular in the nomination network.

**Critical bug found while validating this (unrelated to degree, but blocking):** `Code/02_dyadic_models.R`'s Model 2/3 output (No Religion ≈ 0.16, not significant) did **not** match the numbers already checked into `Tabs/tbl-wave3-reg.tex` and cited throughout `paper.tex` (No Religion ≈ 0.66/0.58, significant). Traced the exact source: the published numbers came from `update_regression_models.R` (deleted in the 2026-09-24 cleanup commit as "superseded ad-hoc scratch" — see the warning in Section 0). That script's `close_num <- as.numeric(as.factor(close))` alphabetizes the four intimacy categories (Distant=1, EspeciallyClose=2, LessThanClose=3, MerelyClose=4) instead of using the true ordinal scale (Distant=1, LessThanClose=2, MerelyClose=3, EspeciallyClose=4) that `Code/02_dyadic_models.R` correctly implements via `case_when`. Confirmed by direct refit that the buggy alphabetical coding exactly reproduces the old published 0.660(0.220) figure. The old ad hoc pooled script also included ego-level `race_1`/`gender_1` main-effect terms (e.g., table rows "Ego: Man", "Ego: African-American") that were never part of the canonical script's design, and the old pooled ad hoc script's `race_h` coding used `TRUE ~ 0` for Foreign Student/Other-race egos instead of `NA` (silently treating them as race-heterophilous rather than excluding them).

**User decision (asked via AskUser 2026-09-24): regenerate the tables from the canonical `Code/02_dyadic_models*.R` scripts, fixing the bug, rather than restoring the old ad hoc spec.** This was implemented:
- Dropped the ego-level `race_1`/`gender_1` fixed-effect rows entirely — they are not part of the canonical script and never were.
- Added `close_num` (correctly ordinal-coded) to the pooled and interaction models, which previously omitted it (for consistency with the Wave 3 spec, since `close` is available in every wave 3-8).
- Rebuilt `Tabs/tbl-wave3-reg.tex` (now 4 columns: Models 1-4, Model 4 = +degree) and `Tabs/tbl-pooled-interaction-reg.tex` (now 4 columns: Model 1 baseline / Model 2 structural / Model 3 +degree / Interaction +degree) entirely from fresh script output. Both tables use `\scriptsize` + `\resizebox{\textwidth}{!}{...}` to fit the extra columns without overfull hboxes.
- Rewrote the Wave 3 regression paragraph, the mediation paragraph, the pooled regression paragraph, and part of the Discussion in `paper.tex` to reflect the corrected numbers: **No Religion is not statistically distinguishable from the Catholic baseline in Wave 3 Models 2-4 (once tie-level structural controls are added), but remains significant in every pooled specification (Models 1-3).** Protestant and Other Religion are robust across all specifications in both Wave 3 and pooled data, including after adding degree controls.
- **Removed the specific ACME/mediation numbers** ($p=0.32$ dorm, $p=0.16$ race) from the paper because no script producing them could be found in git history (see "Still open" above) — replaced with an honest statement that we cannot currently adjudicate mediation vs. rescaling vs. differential-attrition explanations for the Wave 3 Model 1→2 shift.
- Recompiled via `latexmk -pdf paper.tex`: 20 pages, no errors, no undefined refs, no overfull/underfull warnings.

**Current canonical Wave 3 spec (`Code/02_dyadic_models.R`):** `same_religion ~ ego_rel [+ same_gender + same_race + roommates + samedorm + close_num] [+ discuss_num] [+ log_ego_degree + log_alter_pop]`, offset = opportunity_offset, across Models 1-4 respectively. **Current canonical pooled/interaction spec:** same plus clustered SEs (`vcovCL`, cluster = egoid) and (interaction model only) `ego_rel * wave_num`. No ego-level race/gender fixed effects in any canonical model — if these are wanted, they'd need to be deliberately (re-)added and the tables/prose updated again.

---

## 7. Formal Mediation Analysis Rebuilt from Scratch (2026-09-24)

**Task:** replace the removed, unverifiable ACME/mediation claim (Section 6) with a documented, reproducible analysis.

**New script: `Code/03_mediation_analysis.R`.** Installed the `mediation` package (Imai, Keele, and Tingley 2010; not previously installed in this R environment). Because `mediation::mediate()` requires a binary treatment, the analysis is restricted to the Catholic-vs-No-Religion comparison at Wave 3 (Protestant and Other Religion egos dropped for this specific analysis only — they remain in all the multi-group regressions elsewhere). The data-prep block is a deliberate copy of `Code/02_dyadic_models.R`'s Wave 3 prep (same variable definitions, same offset), so the mediation analytic sample lines up with Table `tab:wave3-reg`. Three candidate mediators are tested separately (to avoid post-treatment bias from including one mediator as a "control" in another's model): same-dorm co-residence, roommate status, and race homophily, each net of gender homophily, closeness, religious salience, and ego/alter log-degree. `robustSE = FALSE` quasi-Bayesian simulation (2,000 draws each, three distinct non-sequential seeds) is used; **note this does not cluster by egoid**, a limitation shared with the (also unclustered) Wave 3 regression models it complements.

**Results (`Data/mediation_results.RDS`):**
- Total effect of No Religion (vs. Catholic) on same-religion tie formation, this two-group Wave 3 sample: average effect on the response scale $=0.013$, $p=0.81$ — i.e., **already statistically null**, consistent with the corrected Model 2-4 finding in Section 6.
- ACME (same-dorm) $=-0.004$, 95% CI $[-0.013, 0.002]$, $p=0.21$.
- ACME (roommates) $=-0.001$, 95% CI $[-0.009, 0.005]$, $p=0.72$.
- ACME (race homophily) $=-0.006$, 95% CI $[-0.018, 0.001]$, $p=0.12$.
- None of the three ACMEs is significant, but **because the total effect being decomposed is itself null, this should not be read as "a genuine effect survives unmediated."** It means there is little Catholic/No-Religion disparity left at Wave 3 to explain, structurally or otherwise. This is a materially more cautious conclusion than the earlier (unreproducible) draft's claim of a "genuine, direct association."

**`paper.tex` updated accordingly** (the mediation paragraph in the Wave 3 regression subsection, and the corresponding Discussion sentence) to report these numbers with the total-effect caveat spelled out. Recompiled via `latexmk -pdf paper.tex`: 20 pages, no errors, no undefined refs, no overfull/underfull warnings.

**If reproducing:** run `Code/02_dyadic_models.R` first (or at least confirm its output still matches `Tabs/tbl-wave3-reg.tex`) before trusting `Code/03_mediation_analysis.R`'s total-effect framing, since that framing explicitly depends on the corrected (not the old, buggy) Model 2-4 numbers.

**UPDATE, same day:** the user pointed out (correctly) that decomposing an already-null total effect into ACME components is not very informative — mediation analysis conventionally presumes a real effect worth explaining, and the No Religion Wave 3 total effect is itself statistically null (that's exactly why the ACMEs were all null too). Per the user's explicit choice (asked via AskUser, options ranged from trimming to dropping entirely), **the formal mediation analysis and its ACME/CI numbers were removed from `paper.tex` altogether.** The Wave 3 regression paragraph now just notes that a mediation interpretation of the Model 1→2 shift is hard to establish from nested-model coefficients alone (rescaling artifact, differential sample attrition) without actually running/citing the mediation test; the Discussion paragraph was trimmed similarly. `Code/03_mediation_analysis.R` and `Data/mediation_results.RDS` **still exist in the repo and still run** (useful if a reviewer specifically asks for a formal mediation test, or if a future draft re-establishes a significant total effect worth decomposing) — they are simply no longer cited in the manuscript text. Recompiled clean (20 pages, no errors/warnings) after this trim.

---

## 8. Wave 3 vs. Pooled No Religion Discrepancy — Investigated (2026-09-24)

**Task:** the "still open" item in Section 5 ("pooled No Religion effect vs. null Wave 3 effect are not reconciled") was investigated directly: is this a power/precision issue, or a genuine wave-specific pattern? Ad hoc analysis only (not yet its own script in `Code/`) — reproduce by re-running the `Code/02_dyadic_models_pooled.R` data-prep block, then looping the Model 2 formula (`same_religion ~ ego_rel + same_gender + same_race + roommates + samedorm + close_num + discuss_num`, offset = `opportunity_offset`) over `wave %in% paste0("Wave", 3:8)` separately, plus a clustered `ego_rel * wave_num` interaction term on the full pooled data.

**Finding: not a power artifact.** Refitting the Model 2 spec separately for each wave (rather than pooling) shows the No Religion coefficient is null specifically at Wave 3 ($\beta=0.15$, $p=0.58$; 25 egos, 166 ties — actually *more* egos/ties than any later wave) and then rises across Waves 4–8 ($\beta=0.50$ to $0.96$, mostly $p<0.06$, down to only 11 unique egos by Wave 8). The observed No Religion in-group tie rate climbs steadily from 10.2% (Wave 3) to 29.4% (Wave 8), while the opportunity-implied chance baseline stays flat around 7–11% across waves. So Wave 3 has a genuinely smaller point estimate, not just a noisier one from having fewer observations.

**This pattern is specific to No Religion.** The same per-wave refit for Protestant and Other Religion shows both groups are already significantly homophilous at Wave 3 (Protestant $\beta=0.59$, $p=0.003$; Other Religion $\beta=2.06$, $p<1\times10^{-8}$) and stay roughly flat and significant across all six waves — no comparable rising trajectory. So this isn't a generic "everyone's homophily strengthens with time in college" story; it's particular to the No Religion group.

**But a formal trend test is inconclusive.** A linear `ego_rel * wave_num` interaction fit on the full pooled data with clustered (egoid) SEs gives $\beta=0.093$, SE$=0.079$, $p=0.24$ for the No Religion × wave term — not statistically significant, likely underpowered given only ~11–25 unique No Religion egos in any given wave (repeated observation of the same shrinking set of people across waves doesn't add much independent information, the same limitation noted for Other Religion in Section 3/6).

**Manuscript update:** `paper.tex`'s Discussion (§ "The evidence for the No Religion group is more mixed...") now reports this wave-by-wave breakdown, explicitly stating that Wave 3 looks like a genuinely distinctive cross-section for No Religion (not merely underpowered) while flagging that the formal interaction test is non-significant, so the apparent developmental trend is presented as a hypothesis for future work rather than a confirmed finding. Recompiled via `latexmk -pdf paper.tex`: 20 pages, no errors, no undefined refs.

**If reproducing/promoting to a script:** this analysis currently lives only in an interactive R session; if it's going to be cited more formally (e.g., a supplementary table or figure), it should be written up as its own `Code/0X_wave3_vs_pooled_norelig.R` for reproducibility, following the pattern of the existing `Code/02_dyadic_models*.R` scripts.

## 9. Pooled Analysis Designated as Primary Test; Mediation Removed; Figures Made Reproducible (2026-09-24)

**Task:** an audit (subagent-assisted) of how `paper.tex`'s structure maps onto its core MDH-vs-MEH hypothesis test found three issues: (1) Wave 3 and pooled regressions were presented as co-equal tests with an unreconciled disagreement on No Religion, rather than one being clearly primary; (2) `Code/03_mediation_analysis.R` existed and ran, but `paper.tex:153` explicitly disclaimed pursuing mediation, and `README.md` still claimed the opposite; (3) `Code/01_longitudinal_descriptives.R`, `04_plot_opportunity_stability.R`, and `05_plot_active_coefficients.R` saved plots under different filenames (`plot_*.png`) than the `fig-*.png` files actually `\includegraphics`-ed in `paper.tex` — those figures could not be regenerated from the current pipeline. The user chose: designate pooled as the main analysis, drop mediation entirely, regenerate the main figure from reproducible scripts, and move Yule's Q + opportunity-stability into a "Preliminary Checks" subsection.

**Changes made:**
- **`paper.tex` restructured.** Results section is now: §3.1 "Preliminary Checks" (two `\subsubsection`s: Opportunity Structure Stability, Baseline Mixing Patterns/Yule's Q) → §3.2 "Primary Analysis: Pooled Longitudinal Regressions..." (opens by explicitly stating the pooled model, not Wave 3, is the decisive test of MDH vs. MEH) → §3.3 "Robustness Check: Cross-Sectional Regressions at Wave 3" (opens by framing itself as checking whether the pooled pattern already holds at the earliest wave; explicitly notes where it corroborates the pooled result — Protestant, Other Religion — and where it diverges — No Religion). The Discussion's Wave 3/pooled No Religion paragraph was reworded to state the pooled result is primary and therefore not undercut by the Wave 3 divergence, while still reporting the same wave-by-wave decomposition numbers as before (unchanged: Wave 3 $\beta=0.15,p=0.58$; Waves 4-8 rising to $\beta=0.50$-$0.96$; linear trend test $p=0.24$, non-significant).
- **Table/figure include order in `paper.tex` reordered to match:** opportunity-stability figure → Yule's Q figure → Yule's Q permutation-null table → **pooled/interaction table** → **active-coefs figure** → Wave 3 table → wave-by-wave-norelig figure. Table/figure captions updated with "Preliminary Check:" / "Primary Analysis:" / "Robustness Check:" prefixes (`Tabs/tbl-pooled-interaction-reg.tex`, `Tabs/tbl-wave3-reg.tex`, and the two preliminary-check figure captions in `paper.tex`).
- **Mediation removed.** The paragraph in the (now) Wave 3 robustness-check section already disclaimed pursuing formal mediation (unchanged in substance, lightly reworded); the ACME-specific aside was dropped since the script no longer exists. `Code/03_mediation_analysis.R` and `Data/mediation_results.RDS` were moved to `Archive/orphaned_scripts/` and `Archive/orphaned_data/` respectively (not deleted — recoverable via git history/Archive if a reviewer ever demands a formal mediation test). `README.md`'s package list dropped `mediation`; its methodological-notes bullet claiming "The `mediation` package is used to test (and reject) structural foci..." was replaced with an accurate statement that no mediation analysis is currently cited in the manuscript, with a pointer to the archived script.
- **Figure reproducibility gap closed.** `Code/01_longitudinal_descriptives.R`, `Code/04_plot_opportunity_stability.R`, and `Code/05_plot_active_coefficients.R` now `ggsave()` directly to `Plots/fig-yules-q.png`, `Plots/fig-opportunity-stability.png`, and `Plots/fig-active-coefs.png` respectively — the exact filenames `\includegraphics`-ed in `paper.tex`. All three were re-run this session and the figures regenerated cleanly from the current, canonical scripts (previously these three PNGs could only be reproduced by a now-deleted `run_plot_update.R`). `README.md`'s pipeline section was updated to flag which steps are primary/robustness/preliminary and to note steps 2/6 now save directly to the manuscript's figure paths.
- Recompiled via `latexmk -pdf paper.tex`: **21 pages**, no errors, no undefined refs; one pre-existing cosmetic overfull-hbox warning (2.5pt, unrelated to this session's edits) remains.

**If reproducing:** run scripts in the order given in the updated `README.md` §4. The pooled model (`Code/02_dyadic_models_pooled.R` → `Tabs/tbl-pooled-interaction-reg.tex` → `fig-active-coefs.png`) is now explicitly the primary deliverable; Wave 3 and Yule's Q/opportunity-stability are secondary/preliminary and should be described as such in any future edits.

---

**UPDATE 2026-09-24 (later, superseded — see Section 10) — promoted to scripts, and a supplementary figure added.** Two new scripts formalize this analysis:
- **`Code/08_wave_by_wave_norelig.R`** — reproduces the per-wave Model 2 refits (structural + salience controls, no degree terms) for all three non-Catholic groups (No Religion, Protestant, Other Religion), the descriptive observed-vs-baseline in-group tie rates by group/wave, and the clustered `ego_rel * wave_num` trend-test interaction. Saves `Data/wave_by_wave_norelig_results.RDS` (full model objects) and `Data/wave_by_wave_norelig_summary.csv` (tidy per-wave coefficient + descriptive table). This is the canonical, reproducible source for every wave-by-wave number cited in `paper.tex`'s Discussion (Section 8 above).
- **`Code/09_plot_wave_by_wave_norelig.R`** — reads that summary CSV (regenerating it via `08_wave_by_wave_norelig.R` if missing) and plots per-wave coefficients with 95% CIs for the three non-Catholic groups, saved to `Plots/fig-wave-by-wave-norelig.png`. Added to `paper.tex` as a new supplementary figure (`\label{fig:wave-by-wave-norelig}`, referenced from the Discussion's No Religion paragraph). The plot visually shows No Religion starting lowest and near zero at Wave 3, ending highest at Wave 8, but non-monotonic in between (dips at Waves 6-7) — consistent with the non-significant formal trend test (p = 0.24): suggestive but not statistically confirmed.
- Recompiled via `latexmk -pdf paper.tex`: now **21 pages** (up from 20), no errors, no undefined refs.

---

## 10. Wave 3 Robustness Check and Wave-by-Wave Decomposition Removed Entirely (2026-09-24)

**Task:** the user judged that the Wave 3 cross-sectional robustness check (§3.3 in the prior structure) added "weird qualifications and a lot of repetition" without changing the paper's substantive conclusions, and asked to remove it, then have the rest of the manuscript audited for consistency. Asked via AskUser whether to also remove the related wave-by-wave decomposition (Section 9 above) — user chose to remove both.

**Before editing, fetched and merged two new Overleaf commits** (`0a5fd2d..14c3dc2` on `overleaf/main`) that had landed since the last sync: prose-only rewording of the abstract, Yule's Q paragraph, pooled-regression paragraph (added a Blau's Multiform Heterogeneity framing for the dorm/race findings, with a new `\citep{feld2009}` elaboration and a footnote moving the degree-control aside out of the main text), and Wave 3 paragraph (also prose-only, no numbers changed) — plus deletion of three stray root-level files (`Data/longitudinal_yules_q.csv`, `Data/yules_q_latex.tex`, `Data/yules_q_markdown.md`, `Rplots.pdf`) that were already gone from the local tree, so the merge was conflict-free. Confirmed `git fetch overleaf && git diff HEAD overleaf/main` shows nothing outstanding before starting this session's edits — same lesson as Section 0: always re-check immediately before editing, since a co-author can push mid-session.

**What was removed from `paper.tex`:**
- The abstract's sentence "*A cross-sectional robustness check at the earliest wave corroborates this pattern for Protestant and Other Religion students but not for the No Religion group, whose homophily strengthens only in later waves.*"
- The entire §3.3 "Robustness Check: Cross-Sectional Regressions at Wave 3" subsection (four paragraphs covering Models 1-4, the Other Religion bootstrap caveat, and the unresolved Wave 3/pooled discrepancy for No Religion).
- The Discussion's "No Religion is more mixed" paragraph, which had contained the full wave-by-wave decomposition narrative (Wave 3 β=0.15 rising to β=0.50-0.96 by Wave 8, the non-significant linear trend test, the `Figure~\ref{fig:wave-by-wave-norelig}` citation, and the dorm-covariate aside that duplicated the Primary Analysis section). This was merged into the preceding "non-Catholic students collectively" paragraph, which now states plainly that No Religion is significant across every pooled specification — no caveats about Wave 3 divergence remain, since there is no longer a Wave 3 comparison in the paper.
- The `\include{Tabs/tbl-wave3-reg.tex}` and the `fig-wave-by-wave-norelig` figure block (with their preceding `\newpage`s).
- One stray `\ref{tab:wave3-reg}` left inside `Tabs/tbl-yules-q-null.tex`'s footnote (comparing Yule's Q z-scores to "the bootstrap sensitivity analysis reported with the dyadic regression tables (Tables~\ref{tab:wave3-reg} and \ref{tab:pooled-interaction-reg})") — caught by a `latexmk` undefined-reference warning after the first recompile; fixed to cite only `tab:pooled-interaction-reg`.

**Full-document consistency pass (abstract through Discussion):** re-read every section fresh (Introduction, Study Setting, Methods/opportunity-offset formulation, Results §3.1-3.2, Discussion, limitations) and grepped for `wave 3|wave3|wave-by-wave|robustness|corroborat|diverg|mixed evidence` to confirm no other paragraph still referenced the removed analysis or its numbers. Only one remaining "Wave 3" mention survives, in the Methods §2.1 data-collection description ("*Beginning with Wave 3, respondents were presented with their previously nominated alters...*") — this is describing the survey instrument design, not the removed analysis, and was left untouched. No other claims (group percentages, pooled coefficients, bootstrap CI language, Yule's Q text, mixed-effects-vs-MLM methodological section, limitations) depended on the removed material and none needed further edits.

**Files archived (matching the existing `Archive/orphaned_*` convention from Section 0), not deleted:**
- `Tabs/tbl-wave3-reg.tex` → `Archive/orphaned_tables/`
- `Plots/fig-wave-by-wave-norelig.png` → `Archive/orphaned_plots/`
- `Code/02_dyadic_models.R` (produced the Wave 3 table), `Code/08_wave_by_wave_norelig.R`, `Code/09_plot_wave_by_wave_norelig.R` (produced the decomposition) → `Archive/orphaned_scripts/`. Confirmed none of these are `source()`-d by any other script (only referenced in code comments and `README.md` prose) before moving, so archiving them breaks nothing.
- `README.md` updated: pipeline section renumbered (now 6 steps, was 7), the "Wave 3 is a robustness check" framing removed, and a new paragraph added documenting the 2026-09-24 removal and where the orphaned files now live.

**Current state:** the manuscript's only regression-based hypothesis test is the pooled Waves 3-8 analysis (`Code/02_dyadic_models_pooled.R` → `Tabs/tbl-pooled-interaction-reg.tex`); Yule's Q and opportunity-stability remain preliminary checks (§3.1); there is no cross-sectional robustness check and no wave-by-wave temporal decomposition anywhere in `paper.tex`. Recompiled via `latexmk -pdf paper.tex`: **18 pages** (down from 21), no errors, no undefined refs; the one pre-existing cosmetic overfull-hbox warning (2.5pt, same one noted in Section 9) remains, unrelated to this session's edits.

**If a future draft wants either analysis back:** the underlying scripts and data still exist and still run from `Archive/orphaned_scripts/`; they were removed from the manuscript for narrative-economy reasons stated by the user, not because they were found to be wrong or unreproducible (contrast with the mediation analysis in Section 7, which was removed for a substantive reason — decomposing an already-null total effect).

---

## 11. On-Campus Family Ties Excluded from the Analytic Sample (2026-09-24)

**Trigger:** a question about whether analyses were restricted to on-campus ties (not parents/siblings) surfaced that `altrelucat == "Student"` — the filter shared by every canonical script — is not a pure proxy for "friend": within the 17,452 rows passing that filter, 312 are tagged `family == TRUE` (an on-campus sibling nominating/being nominated by their sibling-ego), alongside 456 `RomanticPartner`, 318 `Other`, and 287 `Acquaintance` ties. The user asked to remove the on-campus family ties specifically (not romantic partners/acquaintances/other, which remain in the sample).

**Fix:** added `filter(family == FALSE)` immediately after `filter(altrelucat == "Student")` in all six scripts that build the tie-level analytic sample from raw `NetWorkSurvey.csv`: `Code/01_longitudinal_descriptives.R`, `Code/02_dyadic_models_pooled.R`, `Code/02_dyadic_models_interaction.R`, `Code/04_plot_opportunity_stability.R`, `Code/05_plot_active_coefficients.R`, `Code/07_yules_q_permutation_test.R`. Confirmed `family` has no missing values within the `Student` subset (17,140 `FALSE` / 312 `TRUE`), so this is a clean drop with no NA-handling ambiguity.

**Re-ran the full pipeline** (all six scripts above) to regenerate `Plots/fig-yules-q.png`, `Plots/fig-opportunity-stability.png`, `Plots/fig-active-coefs.png`, `Tabs/tbl-yules-q-null.tex`, `Data/pooled_clusted_models.RDS`, `Data/interaction_model_results.RDS`, and manually rewrote `Tabs/tbl-pooled-interaction-reg.tex` (this table is **not** written by any script — it was hand-built from console output in an earlier session and must be manually kept in sync; there is no script that emits it directly, unlike `tbl-yules-q-null.tex`).

**Effect on results — small, no qualitative reversal of the paper's core claims, but two structural covariates lost significance in the fully-adjusted (degree-controlled) model:**
- Pooled Model 1-3 group coefficients shifted only slightly (e.g., No Religion 0.549-0.556 vs. the old 0.570-0.577; Other Religion 1.718-1.727 vs. 1.758-1.775; Protestant 0.496-0.497 vs. 0.522-0.526) — all remain significant at $p<0.01$ as before.
- **Same-dorm co-residence and race homophily, in Model 3 (the fully-adjusted, degree-controlled model) only, moved from significant ($p<0.05$) to marginal ($p\approx0.053$ and $p\approx0.053$ respectively).** They remain significant in Model 2 (without degree controls). `paper.tex` was reworded to report Model 2 as the primary citation for these two effects and flag Model 3's marginal status explicitly, rather than silently keeping a `p<0.05` claim that no longer holds under the corrected sample.
- Analytic-sample sizes in the fully-adjusted pooled model dropped modestly: Catholic 327→326 egos / 7,580→7,393 ties; No Religion 37→37 egos / 676→670 ties; Other Religion 19→19 egos / 350→348 ties; Protestant 46→46 egos / 1,114→1,111 ties. (The Other Religion group's total unique-egos-across-the-panel figure cited in prose, 22, is from the less-restrictive baseline sample and is unaffected — verified unchanged after the family-tie filter.)
- Yule's Q and permutation-null values shifted by a few thousandths/hundredths per wave/group (see `Tabs/tbl-yules-q-null.tex`); no group's significance pattern changed.

**Bug fixed incidentally:** `Code/07_yules_q_permutation_test.R`'s hardcoded table-footnote text (and a header comment) still referenced `\ref{tab:wave3-reg}`, a label that was removed from `paper.tex` in Section 10 — this had been patched directly in the checked-in `.tex` output that session but **not in the R script that generates it**, so re-running the script silently reintroduced the undefined-reference warning. Fixed at the source (script) this time so it won't recur on the next re-run.

**`paper.tex` recompiled via `latexmk -pdf paper.tex`: 18 pages, no errors, no undefined refs.**

**RESOLVED 2026-09-24 (same day) — see Section 12.** The Discussion's pooled bootstrap sensitivity analysis, previously ad hoc and unscripted, now has a canonical script (`Code/10_bootstrap_sensitivity.R`) and was re-run on the family-tie-excluded sample; `paper.tex` was updated with the freshly computed numbers.

---

## 12. Pooled Bootstrap Sensitivity Analysis Promoted to a Script (2026-09-24)

**Task:** the Discussion's bootstrap sensitivity paragraph (flagged as a known gap at the end of Section 11) had no corresponding script — it was run interactively in a past session and its numbers ("473 unique egos... 6-10 percentage points above baseline") were typed directly into `paper.tex`, never re-verified after the on-campus-family-tie exclusion. Wrote `Code/10_bootstrap_sensitivity.R` to make this reproducible and re-ran it.

**Method:** an ego-level cluster bootstrap (2,000 reps, seed 6284), resampling unique egos **with replacement, separately within each religious group**, so that a resampled ego's ties across all waves move together as one block (respecting within-ego clustering across waves, matching the original ad hoc analysis's stated design). Uses the same Model 1 (baseline, no structural covariates) data-prep block as `Code/02_dyadic_models_pooled.R`, so the sample is consistent with the pooled regression table. For each replicate, computes each group's **excess in-group tie rate** = observed same-religion tie rate minus the wave-specific opportunity/chance baseline (`prop`, the same quantity used to build the logit offset) — this statistic is already baseline-adjusted and thus comparable across groups of very different sizes, unlike a raw tie rate or a log-odds coefficient. Reports each group's 95% percentile CI and the pairwise-difference 95% CI between every pair of groups (valid since each group's bootstrap is drawn independently). Saves `Data/bootstrap_sensitivity_results.RDS`.

**Results, on the corrected (family-tie-excluded) sample — 472 unique egos, 10,046 ties, Waves 3-8 pooled (Model 1 baseline sample, not the fully-adjusted structural/degree sample):**

| Group | n egos | n ties | Observed rate | Baseline rate | Excess (bootstrap mean, 95% CI) |
|---|---|---|---|---|---|
| Catholic | 348 | 7,630 | 0.835 | 0.797 | 0.038 (0.021, 0.055) |
| No Religion | 52 | 850 | 0.226 | 0.089 | 0.138 (0.084, 0.199) |
| Other Religion | 22 | 381 | 0.155 | 0.026 | 0.125 (0.027, 0.275) |
| Protestant | 50 | 1,185 | 0.181 | 0.088 | 0.094 (0.049, 0.146) |

Pairwise differences (95% CI): Catholic vs. No Religion and Catholic vs. Protestant both **exclude zero** (both minority groups' excess is reliably larger than Catholic's own small excess); Catholic vs. Other Religion, No Religion vs. Other Religion, No Religion vs. Protestant, and Other Religion vs. Protestant **all include zero** (no reliable pairwise distinction, including — notably — between Other Religion and the Catholic majority itself).

**This confirms the substance of the old ad hoc claim but corrects its specific numbers.** The old text said Protestant/No Religion showed "approximately 6-10 percentage points" of excess; the rerun shows Protestant $\approx 9$pp and No Religion $\approx 14$pp — No Religion's true excess is noticeably larger than the old "6-10" range implied, and the two are not actually identical to each other (though the direct No-Religion-vs-Protestant pairwise CI still includes zero, so they remain statistically indistinguishable from each other, just not from a shared point estimate). The number of unique bootstrapped egos is 472, not 473 (a trivial, likely just-rounding-era discrepancy). `paper.tex`'s Discussion paragraph was rewritten to cite the precise bootstrap means ($\approx 9$pp Protestant, $\approx 14$pp No Religion, $\approx 4$pp Catholic, $\approx 13$pp Other Religion) and to state explicitly that Other Religion is statistically indistinguishable from *every* other group, including Catholic — which the pairwise CIs directly support, correcting a claim that was previously asserted without a matching reproducible test.

**`paper.tex` recompiled via `latexmk -pdf paper.tex`: 18 pages, no errors, no undefined refs.**

**UPDATE, same day — promoted to a full supplementary table.** `Code/10_bootstrap_sensitivity.R` now also writes `Tabs/tbl-bootstrap-sensitivity.tex` directly (script-generated, not hand-built — unlike `tbl-pooled-interaction-reg.tex`, this one will never drift out of sync with a re-run). The table has two panels: Panel A reports each group's $n$ egos/ties, observed rate, baseline rate, and bootstrap excess with 95% CI; Panel B reports all six pairwise differences with 95% CIs and an explicit "Excludes 0" column. `paper.tex` now `\include`s it (as Table 3, right after the pooled/interaction table) and the Discussion paragraph points to it (`Table~\ref{tab:bootstrap-sensitivity}`) rather than relying solely on prose point estimates. Recompiled via `latexmk -pdf paper.tex`: **19 pages**, no errors, no undefined refs.

**UPDATE, same day — Panel A footnote added.** `Code/10_bootstrap_sensitivity.R`'s Panel A now includes a footnote noting that the Catholic majority's own excess (bootstrap mean $\approx 3.8$pp, 95% CI excludes zero) is itself reliably nonzero, i.e. even the majority group shows some in-group tie preference beyond chance; the footnote reframes the substantive question as the *relative magnitude* of excess across groups (which Panel B addresses), not mere departure from chance (which all four groups show, cross-referencing Table~\ref{tab:yules-q-null}).

---

## 13. Table 2 (Pooled/Interaction Regression) Made Fully Reproducible (2026-09-24)

**Task:** `Tabs/tbl-pooled-interaction-reg.tex` was the last table in the manuscript with no corresponding script — it had been hand-assembled from console output across several earlier sessions and needed to be manually re-typed every time the underlying models changed (as happened after the family-tie exclusion in Section 11). Wrote `Code/02b_build_pooled_table.R` to close this gap.

**Design:** rather than re-implementing the data-prep/model-fitting logic a third time (risking drift from the two canonical model scripts), `02b_build_pooled_table.R` simply `source()`s `Code/02_dyadic_models_pooled.R` and `Code/02_dyadic_models_interaction.R` in sequence, then builds the table purely from the resulting objects (`m1_clustered`, `m2_clustered`, `m3_clustered`, `clustered_results`, `df_pooled_clean`). A `row_specs` list maps each LaTeX row label to its R coefficient name, and a small helper (`fmt_cell`) formats `estimate<stars> (SE)` or `---` for terms absent from a given model, so adding/removing a covariate in the underlying model scripts automatically propagates to the table without hand-editing. Group sample sizes and even the footnote's degree-term $p$-value range ($p \approx 0.03$--$0.05$) are pulled live from the fitted models rather than hardcoded, so the footnote text can't silently go stale either.

**Verification:** ran the new script and confirmed its output is a byte-for-byte match (row values, sample sizes, footnote figures) to the version manually rebuilt in Section 11, other than a cosmetic whitespace-padding difference in the sample-size column that was fixed by adding `trim = TRUE` to `format()`. Recompiled via `latexmk -pdf paper.tex`: clean, no errors, no undefined refs.

**Current state: every table and figure `\include`/`\includegraphics`-ed in `paper.tex` is now generated by a script** (`Code/01`, `02b`, `04`, `05`, `07`, `10`) with no remaining hand-built artifacts. If the pooled/interaction models ever change again, re-run `02_dyadic_models_pooled.R` and `02_dyadic_models_interaction.R` (or just `02b_build_pooled_table.R`, which sources both) to regenerate Table 1's `.tex` file automatically.

---

## 14. Results Restructured: Shared "Robustness Checks" Subsection (2026-09-24)

**Task:** the Yule's $Q$ permutation test (previously embedded in the "Baseline Mixing Patterns" preliminary-check subsubsection) and the pooled bootstrap sensitivity analysis (previously a paragraph inside "Primary Analysis") both address the identical underlying concern — that baseline/opportunity-adjustment corrects a statistic's *bias* across groups of different sizes but not its *precision* — yet they lived in unrelated parts of the Results section with no cross-reference between them. Restructured `paper.tex`'s Results into three subsections that now match the actual analytical logic:

- **§3.1 Preliminary Checks** — unchanged content (Opportunity Structure Stability; Baseline Mixing Patterns/Yule's $Q$), but the Yule's $Q$ subsubsection now only covers the *descriptive* claim (definition, equation, "universal inbreeding homophily") and forward-references the new Robustness Checks subsection for the permutation test itself, rather than presenting the test in place.
- **§3.2 Primary Analysis** — unchanged coefficient-interpretation paragraphs (three-group significance, dorm/race findings, marginal-effects/Figure~3 paragraph), but the paragraph that used to walk through the full bootstrap methodology and results in-line now states the qualitative finding in one sentence and forward-references §3.3 for the supporting analysis.
- **§3.3 Robustness Checks: The Group-Size Precision Problem** (new, `\label{sec:robustness}`) — opens with a short paragraph naming the shared bias-vs-precision distinction, then two subsubsections: "Yule's Q Permutation Test" (the paragraph moved from §3.1, unchanged in substance) and "Bootstrap Sensitivity Analysis of Pooled Coefficients" (the paragraph moved from §3.2, unchanged in substance). Closes with a synthesis paragraph stating explicitly that the two independent diagnostics converge: Catholic/non-Catholic is a reliable distinction, but a finer ranking among the three non-Catholic groups is not.

**Figure/table include order at the end of `paper.tex` was reordered to match:** Preliminary-check figures (`fig-opportunity-stability`, `fig-yules-q`) → Primary Analysis outputs (`tbl-pooled-interaction-reg` as Table 1, `fig-active-coefs`) → Robustness Check tables (`tbl-yules-q-null` as Table 2, `tbl-bootstrap-sensitivity` as Table 3). Both robustness tables' captions were changed from generic ("Supplementary:") to "Robustness Check:" prefixes at the source, in `Code/07_yules_q_permutation_test.R` and `Code/10_bootstrap_sensitivity.R`, for consistency with the "Preliminary Check:"/"Primary Analysis:" prefixes already used on the figures — both scripts were re-run to regenerate the `.tex` files with the new captions.

**No content was cut**, only relocated and given a shared framing; all specific numbers (z-score range 1.3–7.5, bootstrap percentage points, CIs) are unchanged from Sections 11–12. Table numbering shifted (pooled/interaction table is now Table 1, Yule's Q permutation is Table 2, bootstrap sensitivity is Table 3) but all `\ref`/`\label` cross-references were verified to resolve correctly (LaTeX auto-numbers; no hardcoded table numbers exist in the prose). Recompiled via `latexmk -pdf paper.tex`: **20 pages** (up from 19), no errors, no undefined refs.

---

## 15. Overleaf Re-Synced After Sections 11–14 (2026-09-24)

**Task:** verify whether `overleaf/main` had fallen behind local `my-new-branch` after the on-campus-family-tie exclusion (Section 11), the bootstrap-sensitivity script promotion (Section 12), Table 1's reproducibility fix (Section 13), and the Results restructuring (Section 14) — none of which had been pushed to Overleaf yet.

**Findings:** `git fetch overleaf && git diff HEAD overleaf/main` showed `overleaf/main` still at commit `c5d8b0b` (the state from the Section 10 merge), missing: `Code/02b_build_pooled_table.R`, `Code/10_bootstrap_sensitivity.R`, `Tabs/tbl-bootstrap-sensitivity.tex`, and the corresponding `paper.tex`/`paper.pdf`/`AGENTS.md`/`Data`/`Plots` updates from Sections 11–14. `git diff HEAD overleaf/main -- paper.tex` confirmed the differences were entirely prose/numbers that had already been superseded locally (old family-tie-inclusive coefficients, the pre-Section-14 split-apart Yule's-Q-permutation-test / bootstrap-sensitivity subsections, old inline "473 unique egos" bootstrap prose instead of the `Table~\ref{tab:bootstrap-sensitivity}` citation) — **no independent Overleaf-side edits existed to reconcile this time** (contrast with Sections 0 and 10, where a co-author had pushed genuinely new commits mid-session that needed merging).

**Verified via `git merge-base --is-ancestor overleaf/main HEAD`** that `overleaf/main` was a strict ancestor of local `HEAD` before pushing — i.e., this was guaranteed to be a clean fast-forward, not a merge. `git push overleaf` succeeded with a fast-forward (`c5d8b0b..08ae50c my-new-branch -> main`, no conflicts). Re-fetched afterward and confirmed `origin/my-new-branch`, `overleaf/main`, and local `my-new-branch` are all identical at `08ae50c`.

**Lesson reaffirmed:** the "always `git fetch overleaf` and diff immediately before pushing" habit (Sections 0 and 10) caught that Overleaf was stale, but this time the check also revealed there was *nothing to merge* — a good reminder that the fetch-and-diff step matters for two different reasons: to catch a co-author's concurrent edits (Sections 0, 10) and to confirm a plain fast-forward is genuinely safe before pushing (this session). `git merge-base --is-ancestor` is a quick way to distinguish the two cases without eyeballing the diff.

---

## 16. Section 2.4 ("Methodological Advantages of the Dyadic Approach") Moved to an Appendix (2026-09-24)

**Task:** the user judged that former §2.4 (E-I index boundary-bias comparison + mixed-effects-vs-clustered-regression justification) was methodological throat-clearing that interrupted the Methods section's flow into Results, and asked to move it to an appendix.

**Change:** `paper.tex` now has `\appendix` inserted right after `\bibliography{references}` and before the figure/table `\include`/`\includegraphics` block at the end of the document. The two former `\subsubsection`s ("Benefits of the Opportunity Offset" and "Why Dyadic Clustered Regression is Preferable to Mixed-Effects Multilevel Models") are unchanged in content/wording, now living as `\subsection`s under a new `\section{Methodological Advantages of the Dyadic Approach}` labeled `app:methodological-advantages`. In the main text, §2.3 (Opportunity Offset Formulation) now ends with a one-sentence forward-reference: "Appendix~\ref{app:methodological-advantages} details the methodological advantages of this dyadic approach relative to two natural alternatives: aggregated E-I index measures, and mixed-effects multilevel models." No numbers, equations, or citations were altered — pure relocation.

**Recompiled via `latexmk -pdf paper.tex`: 20 pages, no errors, no undefined refs**; the same pre-existing 2.5pt cosmetic overfull-hbox warning (unrelated, noted since Section 9) remains.

**Not yet pushed to Overleaf as of this edit** — remember to `git fetch overleaf && git diff HEAD overleaf/main` before the next push, per the standing lesson in Sections 0/10/15.
